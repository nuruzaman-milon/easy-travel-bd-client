import React from 'react';
import Banner from '../Banner/Banner';

const Home = () => {
    return (
        <div>
            {/* Add the components one by one */}
            <Banner></Banner>
        </div>
    );
};

export default Home;